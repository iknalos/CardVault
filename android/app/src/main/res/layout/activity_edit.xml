<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#0F1220"
    android:fillViewport="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="20dp">

        <TextView
            android:id="@+id/editTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="20dp"
            android:text="New card"
            android:textColor="#FFFFFF"
            android:textSize="24sp"
            android:textStyle="bold" />

        <TextView style="@style/FieldLabel" android:text="Card name" />
        <EditText
            android:id="@+id/fName"
            style="@style/BigField"
            android:hint="e.g. Library card, Gym card"
            android:inputType="textCapSentences"
            android:maxLength="60" />

        <TextView
            android:id="@+id/nfcInfo"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="16dp"
            android:background="@drawable/field_bg"
            android:padding="14dp"
            android:textColor="#9AA1C0"
            android:textSize="16sp"
            android:visibility="gone" />

        <TextView android:id="@+id/labelValue" style="@style/FieldLabel" android:text="Barcode value" />
        <EditText
            android:id="@+id/fValue"
            style="@style/BigField"
            android:hint="Numbers/text in the barcode"
            android:inputType="textVisiblePassword" />

        <TextView android:id="@+id/labelFormat" style="@style/FieldLabel" android:text="Barcode type" />
        <Spinner
            android:id="@+id/fFormat"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:layout_marginBottom="16dp"
            android:background="@drawable/field_bg"
            android:paddingLeft="8dp" />

        <ImageView
            android:id="@+id/previewImage"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="6dp"
            android:adjustViewBounds="true"
            android:maxHeight="200dp"
            android:background="#FFFFFF"
            android:padding="12dp"
            android:visibility="gone" />

        <TextView
            android:id="@+id/previewErr"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="10dp"
            android:textColor="#FF5C7A"
            android:textSize="15sp" />

        <TextView style="@style/FieldLabel" android:text="Color" />
        <LinearLayout
            android:id="@+id/colorRow"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:layout_marginBottom="16dp"
            android:gravity="center_vertical"
            android:orientation="horizontal" />

        <TextView style="@style/FieldLabel" android:text="Notes (optional)" />
        <EditText
            android:id="@+id/fNotes"
            style="@style/BigField"
            android:hint="Anything to remember about this card"
            android:inputType="textCapSentences|textMultiLine"
            android:minLines="2" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/saveBtn"
            android:layout_width="match_parent"
            android:layout_height="64dp"
            android:layout_marginTop="10dp"
            android:text="Save card"
            android:textSize="20sp" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/cancelBtn"
            style="@style/Widget.Material3.Button.TextButton"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:text="Cancel"
            android:textColor="#9AA1C0"
            android:textSize="18sp" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/deleteBtn"
            style="@style/Widget.Material3.Button.OutlinedButton"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:layout_marginTop="20dp"
            android:text="Delete this card"
            android:textColor="#FF5C7A"
            android:textSize="18sp"
            android:visibility="gone"
            app:strokeColor="#FF5C7A" />
    </LinearLayout>
</ScrollView>
